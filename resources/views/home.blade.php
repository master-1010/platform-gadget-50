@extends('layouts.app') @section('content')<section class="py-10"><h1 class="text-4xl font-black mb-6">Latest News</h1><div class="grid md:grid-cols-3 gap-5">@forelse($posts as $post)<article class="bg-white rounded shadow p-5"><p class="text-sm text-blue-700">{{ $post->category->name }}</p><h2 class="text-xl font-bold mt-2"><a href="/news/{{ $post->slug }}">{{ $post->title }}</a></h2><p class="text-slate-600 mt-2">{{ $post->excerpt ?: Str::limit(strip_tags($post->content),140) }}</p><p class="text-xs mt-4">{{ $post->published_at?->toFormattedDateString() }} · {{ $post->views }} views</p></article>@empty <p>No published news yet.</p>@endforelse</div><div class="mt-6">{{ $posts->links() }}</div></section>@endsection
